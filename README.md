# Northcoders News API

You will need to create 2 environment variables to get the database up and running:

A .env.test file with PGDATABASE=nc_news_test 

A .env.development file with PGDATABASE=nc_news